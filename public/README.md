# public/

Drop the official KNX logo here as `knx-logo.svg` (or replace the existing
placeholder file with the same name). It will be used in the header, hero,
footer, and as the favicon automatically.

If your asset is a PNG, save it as `knx-logo.png` and update the `<img src>`
in `app/[locale]/components/Logo.tsx` accordingly.
